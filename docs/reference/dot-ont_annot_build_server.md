# Internal - builds ontology annotation server

Internal - builds ontology annotation server

## Usage

``` r
.ont_annot_build_server(ontologies)
```

## Arguments

- ontologies:

  A list with `hpo`, `ecto`, `chebi` data.frames.

## Value

A Shiny server function.
