# Internal - should we allow network access now?

Controlled by the R option `tidyexposomics.allow_network` and
[`interactive()`](https://rdrr.io/r/base/interactive.html).

## Usage

``` r
.can_use_network()
```

## Value

Logical scalar.
