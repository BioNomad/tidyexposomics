% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_associate_go_outcome.R
\name{plot_associate_go_outcome}
\alias{plot_associate_go_outcome}
\title{Plot GO Group-Outcome Associations}
\usage{
plot_associate_go_outcome(
  expomicset,
  filter_col = "p.value",
  filter_thresh = 0.05,
  direction_filter = "all",
  nrow = 1
)
}
\arguments{
\item{expomicset}{A \code{MultiAssayExperiment} object containing \code{pc_glm_results}.}

\item{filter_col}{A character string specifying the column used for filtering significant associations. Default is \code{"p.value"}.}

\item{filter_thresh}{A numeric value specifying the threshold for filtering significant associations. Default is \code{0.05}.}

\item{direction_filter}{A character string specifying which effect direction to display.
Options are \code{"all"}, \code{"up"} (positive associations), or \code{"down"} (negative associations). Default is \code{"all"}.}

\item{nrow}{An integer specifying the number of rows in the faceted plot layout. Default is \code{1}.}
}
\value{
A \code{ggplot} object displaying the GO group-outcome associations.
}
\description{
Generates a forest plot visualizing significant associations between GO groups and outcomes
from principal component regression results stored in \code{expomicset}.
}
\details{
The function extracts GO group-outcome associations from \code{metadata(expomicset)$pc_glm_results},
filters based on \code{filter_col} and \code{filter_thresh}, and visualizes the effect sizes using a forest plot.
Associations are color-coded as upregulated (red), downregulated (blue), or non-significant (grey).
Faceting is applied to separate results by experiment and cluster.
}
\examples{
\dontrun{
plot_associate_go_outcome(expom)
}

}
