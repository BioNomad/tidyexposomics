% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlate_exposures.R
\name{correlate_exposures}
\alias{correlate_exposures}
\title{Correlate Exposure Variables}
\usage{
correlate_exposures(
  expomicset,
  exposure_cols = NULL,
  threshold = 0.3,
  action = "add"
)
}
\arguments{
\item{expomicset}{A \code{MultiAssayExperiment} object containing exposure variable data.}

\item{exposure_cols}{An optional character vector specifying exposure variables to include. If \code{NULL}, all numeric exposures are used. Default is \code{NULL}.}

\item{threshold}{A numeric value specifying the minimum absolute correlation required to retain results. Default is \code{0.3}.}

\item{action}{A character string indicating whether to return results (\code{"get"}) or add them to metadata (\code{"add"}). Default is \code{"add"}.}
}
\value{
If \code{action = "add"}, returns the modified \code{expomicset} with correlation results stored in metadata.
If \code{action = "get"}, returns a list containing:
\item{correlation_table}{A data frame with all computed correlations.}
\item{filtered_table}{A data frame containing only correlations above the threshold.}
}
\description{
Computes correlations between numeric, categorical, and mixed exposure variables
using Pearson correlation, R-squared from linear models, and Cramer's V.
Supports filtering based on correlation strength.
}
\details{
The function extracts numeric and categorical exposure variables from \code{colData(expomicset)} and computes:
\itemize{
\item \strong{Numeric-numeric correlations} using Pearson correlation.
\item \strong{Numeric-categorical associations} using R-squared from linear models.
\item \strong{Categorical-categorical associations} using Cramer's V.
}

Results are filtered based on the absolute correlation threshold and stored in metadata if \code{action = "add"}.
}
\examples{
\dontrun{
results <- correlate_exposures(
  expomicset = expom,
  exposure_cols = c("PM2.5", "NO2", "lead"),
  threshold = 0.3,
  action = "get"
)
}

}
