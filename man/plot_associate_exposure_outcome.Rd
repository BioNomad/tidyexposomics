% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_associate_exposure_outcome.R
\name{plot_associate_exposure_outcome}
\alias{plot_associate_exposure_outcome}
\title{Plot Exposure-Outcome Associations}
\usage{
plot_associate_exposure_outcome(
  expomicset,
  filter_col = "p.value",
  filter_thresh = 0.05
)
}
\arguments{
\item{expomicset}{A \code{MultiAssayExperiment} object containing ExWAS results.}

\item{filter_col}{A character string specifying the column used for filtering significant associations. Default is \code{"p.value"}.}

\item{filter_thresh}{A numeric value specifying the threshold for filtering significant associations. Default is \code{0.05}.}
}
\value{
A \code{ggplot} object displaying the exposure-outcome associations.
}
\description{
Generates a forest plot visualizing significant exposure-outcome associations
from ExWAS results stored in \code{expomicset}.
}
\details{
The function extracts ExWAS results from \code{metadata(expomicset)$exwas$results_df},
filters based on \code{filter_col} and \code{filter_thresh}, and generates a forest plot of effect sizes.
Associations are color-coded as upregulated (red), downregulated (blue), or non-significant (grey).
}
\examples{
\dontrun{
plot_associate_exposure_outcome(expom)
}

}
